# Landing Page Tests

1. The landing page loads successfully in a browser.
2. The landing page displays the business name "RoseCare" (or your chosen name).
3. The landing page shows a tagline describing the service.
4. A "Sign up for early access" button is visible.
5. Clicking the button does not throw errors (functionality can be added later).
